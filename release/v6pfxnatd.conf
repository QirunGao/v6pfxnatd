# v6pfxnatd does not require a custom kernel module.
# Linux rtnetlink and nftables support are provided by the distribution kernel.
# Operators may preload nf_tables here if their environment requires it.
# nf_tables
